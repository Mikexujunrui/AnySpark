"""
anyspark.review.prompts — 评审团提示词（内容资产）。

与机制分离：机制（panel）不内嵌长提示词；提示词是给 LLM 看的自然语言内容，
行可长（per-file-ignore E501，对齐 align/skillgen 先例）。
"""

# 评审员输出格式要求（JSON schema 描述，模型无关自然语言）
OUTPUT_SCHEMA = """输出格式（严格 JSON 对象，不要加 ``` 标记）：
{
  "scores": {"维度名": 分数(0-10), ...},
  "overall_score": 综合评分(0-10),
  "highlights": ["亮点1", "亮点2", ...],
  "issues": ["问题1", "问题2", ...],
  "suggestions": ["建议1", "建议2", ...],
  "comment": "一段100-200字的整体评价（严格用你的角色语气和口吻写）"
}
规则：
1. 严格按照你的角色人设和评审风格写评价
2. 每个维度独立评分；亮点至少1条、问题至少1条、建议至少1条
3. comment 体现你的个性，禁止模板腔"""

SUMMARIZE_SYSTEM = """你是评审团主席，负责汇总所有评审员的意见。输出严格 JSON 对象（不要加 ``` 标记）：
{
  "summary": "200字以内的综合评价，概括整体质量和关键发现（客观中立）",
  "consensus": ["多数评审员共同认同的观点1", "观点2", ...],
  "divergences": ["评审员之间意见相反或冲突的地方1", ...],
  "top_suggestions": ["从所有建议中选出的最有价值改进建议1", "建议2", "建议3"]
}
规则：
1. summary 客观中立，兼顾专业评审和读者反馈
2. consensus 提取多数评审员都提到的共同问题或优点
3. divergences 找出评审员之间意见相反的地方（没有则给空数组）
4. top_suggestions 选最有价值的 3-5 条"""
